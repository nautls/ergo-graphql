schema: ../../src/graphql/schema.graphql

generates:
  ./src/index.ts:
    plugins:
      - typescript
      - typescript-operations
      - typed-document-node
    config:
      defaultScalarType: unknown
      scalars:
        JSONObject: object
